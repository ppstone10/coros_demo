这里主要是对每个卡片的数据进行单独的修改，修改的场景是在正常数据场景下才有下面的选择内容，其他场景内容仍然存在。

1. 顶部卡片：步数、卡路里、运动时长可输入设置，持久化的内容是步数值、卡路里值、运动时长值

2. 今日活动：可输入总里程、配速，跑步类型根据配速来适配。配速大于8min/km，为轻松跑，配速为5min/km-8min/km，为节奏跑，小于5min/km，为耐力跑，持久化的内容是总里程、配速

3. 本周计划：可针对本周七天进行每一天的设置，可以设置每天的公里数和跑步类型，也可以设置为休息日，跑步类型可以选择轻松跑、节奏跑、耐力跑，然后根据公里数和跑步类型来计算时间和训练负荷，轻松跑的平均配速设置为10min/km，节奏跑的平均配速设置为6min/km，耐力跑的平均配速设置为4min/km，同步计算运动负荷，这里运动负荷根据平均配速来计算，计算方法你来定义一个比较合理的，然后根据每一天的运动负荷反映到右边的柱状图上，持久化的内容是每天的跑步类型和公里数（或保存为休息日）

4. 本周负荷：这里的对应的七日的负荷也可以手动输入，这里的总负荷就是通过七日的负荷相加得到，建议负荷范围则根据当前场景来，对应的右侧柱状图也要根据输入数据来做变化，持久化的内容是每日的负荷值

5. 训练量评估：这里的输入量为短期负荷和长期负荷，负荷比则计算短期负荷/长期负荷，当负荷比大于1.2时，为高效训练，配上对应文字，负荷比为0.8-1.2时，为维持训练，配上一段描述文字，负荷低于0.8时，为低效训练，配上一段描述文字持久化的内容是短期负荷值和长期负荷值

6. 体力恢复：这里输入的是体力的百分比数值，输入为0-100，下方恢复时长的数值需要你来设计一个算法来计算，且在右侧仪表盘中要有对应的高光条的变化，持久化的内容是体力的百分比值

7. 跑步能力：这里输入的是跑步能力的数值，输入为0-100，下方马拉松预测的数值需要你设计一个算法来计算，并显示计算的数值，右方仪表盘中有对应的高光条的变化，持久化的内容是跑步能力数值

8. 骑行TFP：这里输入的是骑行TFP的数值，输入为0-100，下方的骑行类型你来设置几个，根据输入数值的不同来变化，右方仪表盘有对应高光条的变化，持久化的内容是骑行TFP的数值

9. 心率：这里输入的是平均心率的数值，然后可以选择心率的变化幅度，分为正常、异常高、异常低，然后你根据这里的平均心率数值和变化幅度，来计算生成一个每5分钟一个数值的一天的心率数据，持久化的内容是心率的完整数值序列

10. 压力：这里输入平均压力的数值，然后可以选择压力的场景，分为正常、高压，然后你根据这里的平均压力数值和场景，来计算生成一个一天的压力数据（每半小时一个数值），这里需要注意，压力的变换需要比心率缓，算法不要过于离谱，高压场景只是某一段时间保持高压数值状态，持久化的内容是压力的完整数值序列

11. 睡眠：这里输入的是睡眠的时长和入睡时间，结束时间你来计算，然后对于这一整个睡眠的时长做不同情况的划分，这里你可以充分发挥自己的想法来做这个总时长不同睡眠情况的划分，要便于操作，便于调整，你只是做一个模块来让我来设置这个睡眠情况，所以要便于我操作，比如从开始时间给我一个条，我来选择这个状态睡眠多久，然后这里选择完之后再从这里的结束时间开始选择下一个场景，持久化的内容是不同时间段的睡眠状态以及对应的睡眠时间段

12. HRV评估：这里输入的是平均HRV的数值，且你根据这个数值和定义的状态区间来做评估，即47-57是正常范围，37-47是偏低，27-37是严重偏低，57-67是偏高，这样来划分，右边的指针也会根据实际数据指向对应的数值位置，持久化的内容是平均HRV的数值

13. 静息心率：这里输入的是静息心率的值和测量时间的值和近30天平均的值，且在右侧对应指针指在对应位置，这里近30天平均值的文字显示在右边进度条上面，且需要在进度条中画一条虚的竖线来指向对应的位置，持久化的内容是静息心率的值和近30天的平均值

14. 健康快测：这里需要输入的是心率、HRV、压力、呼吸率、血氧和测量时间6个值，持久化的内容是心率、HRV、压力、呼吸率、血氧和时间

15. 体型管理：这里需要选择锻炼的部位，提供不同的肌肉部位选择，选择完之后，在对应的图上，显示锻炼的部位，持久化的内容是当前的体重和锻炼的肌肉部位