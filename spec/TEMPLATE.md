# <功能名称> Spec

## 元数据

- Spec ID 前缀：`<DOMAIN-TOPIC>`
- 状态：草案 / 已采纳 / 已废弃
- 负责人：<姓名或角色>
- 关联需求：<链接、Issue 或本地文档；没有则写“无”>
- 最后更新：YYYY-MM-DD

## 目标

- <本次必须交付的用户价值或工程结果>

## 非目标

- <明确不在本次范围内的能力，防止实现外溢>

## 边界与约束

- 架构边界：<共享层/平台层职责>
- 安全与数据边界：<禁止使用的真实数据、接口或敏感信息>
- 兼容性边界：<平台、版本、迁移限制>
- 失败边界：<允许降级还是必须失败>

## 数据与状态

| 名称 | 类型/结构 | 来源 | 生命周期 | 约束 |
|------|-----------|------|----------|------|
| `<name>` | `<type>` | `<source>` | `<lifecycle>` | `<constraint>` |

如涉及持久化或跨端数据，写明 `.proto` 字段、domain model 和存储映射边界。

## 行为规范

### `<DOMAIN-TOPIC-001>`：<行为标题>

- Given：<前置状态>
- When：<操作或事件>
- Then：<可观察结果>
- 异常/边界：<错误、空数据、重复操作、恢复行为>

### `<DOMAIN-TOPIC-002>`：<行为标题>

- Given：<前置状态>
- When：<操作或事件>
- Then：<可观察结果>
- 异常/边界：<错误、空数据、重复操作、恢复行为>

## 测试要求

| Spec ID | 自动化测试/人工验收 | 预期结果 |
|---------|---------------------|----------|
| `<DOMAIN-TOPIC-001>` | `<测试名或验收步骤>` | `<断言>` |
| `<DOMAIN-TOPIC-002>` | `<测试名或验收步骤>` | `<断言>` |

实现前先写测试并运行红灯。无法自动化时，在此写清原因和可重复的人工验收步骤。

## 验收标准

- [ ] 所有规范 ID 已在 `spec/TRACE.md` 建立映射。
- [ ] 测试能够捕获缺失行为，并在实现后通过。
- [ ] 正常、边界、错误和恢复场景按本次范围完成。
- [ ] 没有引入“非目标”中的行为。
- [ ] TRACE、`Codex_worklog.md` 和必要的 `LEARNINGS.md` 已更新。

## 待人工确认

- <需要业务、设计、安全或平台负责人决定的问题；没有则写“无”>

